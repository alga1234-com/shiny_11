library(shiny)

fluidPage(
  titlePanel("Hello with UI/Server Split"),
  sidebarLayout(
    sidebarPanel(
      sliderInput("num", "Choose a number", 1, 100, 30)
    ),
    mainPanel(
      textOutput("text")
    )
  )
)
